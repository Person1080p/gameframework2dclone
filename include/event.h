#ifndef __EVENT_H__
#define __EVENT_H__

#include <stdio.h>
#include <stdlib.h>
#include "simple_logger.h"

char *event_rand_level(int min, int max);

char *event_rand_chest(int min, int max);

#endif
